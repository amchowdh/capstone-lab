{
  "name": "backend",
  "version": "1.0.0",
  "description": "Trivia Night backend — Express + in-memory models",
  "main": "src/index.js",
  "scripts": {
    "start": "nodemon src/index.js",
    "test": "jest --detectOpenHandles",
    "coverage": "jest --coverage --coverageReporters=json-summary --coverageReporters=text-summary"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
